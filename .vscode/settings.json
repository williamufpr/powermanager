{
    "jupyter.notebook.fileRoot": "${workspaceFolder}",
// Força o uso do ambiente virtual (substitua ".venv" se o nome for outro)
    "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python"
}

